;-*-scheme-*-

(define (simple-page ttl . elems)
  `((html (head (title ,ttl))
	  (body (@ (bgcolor "white"))
		(h1 ,ttl)
		,@elems))))

(define (hello-world)
  (simple-page "Hello, world!"))

(initialize-main-proc hello-world)
